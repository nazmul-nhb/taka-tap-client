import { Helmet } from "react-helmet-async";

const Profile = () => {
    return (
        <section>
            <Helmet>
                <title>User Profile - TakaTap</title>
            </Helmet>
            Hi from Profile!
        </section>
    );
};

export default Profile;